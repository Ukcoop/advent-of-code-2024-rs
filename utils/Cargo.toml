[package]
name = "utils"
version = "0.1.0"
edition = "2021"

[dependencies]
csv = "1"

[lints.clippy]
needless_return = "allow"

unwrap_used = "deny"
expect_used = "deny"
panic = "deny"

redundant_clone = "deny"
implicit_clone = "deny"
cloned_instead_of_copied = "deny"
